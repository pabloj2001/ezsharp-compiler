// Hello world
def int add(int x, int y)
    int z;
    return x + y;
fed;

int k, x, c, a[10], b;
k = -23;
x = 3 + 2;

// if with brackets
if ((x + 1) < 3 and (x / 3 > 2)) then
    print add(add(x, 3), 2);
fi;

a[2] = 3;.