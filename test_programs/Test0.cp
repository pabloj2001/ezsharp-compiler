// Hello world
def int add(int x, int y)
    int z;
    return x + y;
fed;

int x, c, a[(5 > 3) * 3], b;
double d[1];
c = -23;
x = 3 + 2;

// if with brackets
while ((x + a[0]) < 3 and (x / 3 > 2)) do
    print add(add(x, 3), 2);
od;

a[1] = 2 + a[x * 3];.